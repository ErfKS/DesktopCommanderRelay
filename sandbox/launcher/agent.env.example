# Local credentials for the Windows sandbox agent.
# Copy this file to agent.env and fill in the real values.
#
# agent.env is intentionally ignored by Git.

RELAY_WS_URL=wss://your-relay.example.com/agent
AGENT_TOKEN=replace-with-your-agent-token
